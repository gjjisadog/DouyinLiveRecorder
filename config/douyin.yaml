# Docker daemon configuration. Replace the example URL with the room you record.
rooms:
  - url: https://live.douyin.com/123456
    name: example
    quality: original
    enabled: true

recorder:
  format: ts
  segment_seconds: 1800
  poll_seconds: 120
  max_concurrent_checks: 3
  stream_protocol: auto

storage:
  path: /data/downloads
  state_path: /data/state
  min_free_gb: 10

proxy:
  url: ""

cookie:
  value: ""

notifications:
  enabled: false
