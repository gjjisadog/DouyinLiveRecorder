# 2026-04-03 BL-018 缩比近真阈值长时观察

## 本次做了什么
- 直接执行 `BL-018`，用打包版 EXE 做一轮“接近真实目标但可在当前会话内完成”的长时观察。
- 观察命令：

```powershell
.\.client-conda-env\python.exe scripts\exe_automation_acceptance.py --repo-root . --workspace .\tmp_bl018_near_real_threshold --record-seconds 420 --max-file-size-gb 0.01 --poll-interval 1.0
```

## 为什么不是直接跑 1GB
- 当前测试流速下，真实 `1 GB` 单段观察需要数小时，不适合本次会话内直接完成。
- 因此本轮明确采用 `0.01 GB` 作为缩比近真样本。
- 这能回答“`BL-016` 的安全余量在更高阈值下是否仍然有效”，但不能直接替代真实 `1 GB` 结论。

## 关键结果
- 产物：`tmp_bl018_near_real_threshold/acceptance_summary.json`
- 参数：
  - `record_seconds=420`
  - `max_file_size_gb=0.01`
  - `poll_interval=1.0`
- 阈值：`10737418` bytes
- 切段结果：
  - 总输出文件数：4
  - 历史记录数：4
  - 前 3 段大小：
    - `10897420`
    - `10897420`
    - `11041240`
  - 最后一段为停止后的尾段：`334076`
- 超限结果：
  - 最大超限偏差：`303822` bytes
  - 最大超限占比：约 `2.83%`

## 结论
- `BL-016` 的固定 `1 MB` 安全余量在 `0.01 GB` 这一档仍然有效。
- 相比 `0.001 GB` 压力样本，这一档的切段频率明显回落，不再出现过早切段过密的问题。
- 当前状态已经从“功能正确但精度不可接受”进入“可用且可继续调优”的阶段。

## 仍未确认
- 真实 `1 GB` 阈值下：
  - 当前 `1 MB` 安全余量是否仍然合适
  - 最终超限比例是否还能维持在可接受范围
  - 切段频率是否符合最终预期

## 建议下一步
- 新建或继续执行 `BL-019`，直接做真实 `1 GB` 长时观察。
- 若无法在单次会话内完成，可考虑：
  - 独立跑 overnight 长测
  - 只保留自动化桥与结果汇总，降低人工参与成本
