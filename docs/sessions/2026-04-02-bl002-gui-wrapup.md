# 2026-04-02 BL-002 GUI Wrap-up

## 本次做了什么
- 在隔离工作区 `tmp_bl002_gui_acceptance/` 启动了一轮真实 Qt 界面验收。
- 通过真实“新增任务”弹窗新增了 1 条直链录制任务。
- 验证了任务页右键菜单的状态联动：
  - 空闲时：`启动任务` 可用、`停止任务` 不可用、`编辑任务` 可用
  - 运行中：`启动任务` 不可用、`停止任务` 可用、`编辑任务` 不可用
- 通过任务行内按钮真实完成：
  - 启动任务
  - 等待录制
  - 停止任务
- 观察并记录了任务页状态高亮、状态标签、日志页统计、历史页落档。

## 关键取证
- 工作区：`tmp_bl002_gui_acceptance/`
- 验收摘要：`tmp_bl002_gui_acceptance/acceptance_summary.json`
- 录制产物目录：`tmp_bl002_gui_acceptance/downloads/`
- 历史记录：`tmp_bl002_gui_acceptance/client_data/history.json`
- 任务存储：`tmp_bl002_gui_acceptance/client_data/tasks.json`

## 已确认结果
- 任务新增弹窗链路可用：
  - 已新增 `task-001`
  - 平台识别为 `直链`
  - 显示名称为 `BL002_GUI_Acceptance`
- 任务页右键菜单状态联动符合预期：
  - 空闲态：
    - `启动任务 = true`
    - `停止任务 = false`
    - `编辑任务 = true`
    - `删除任务 = true`
  - 运行态：
    - `启动任务 = false`
    - `停止任务 = true`
    - `编辑任务 = false`
    - `删除任务 = true`
- 行内按钮联动符合预期：
  - 启动后：
    - 任务状态 = `running`
    - 状态标签 = `运行中`
    - 行高亮颜色 = `#ecfdf3`
    - 开始按钮禁用、停止按钮启用、编辑按钮禁用
  - 停止后：
    - 任务状态 = `stopped`
    - 状态标签 = `已停止`
    - 开始按钮恢复可用、停止按钮禁用、编辑按钮恢复可用
- 日志页联动已确认：
  - 本轮日志共 `18` 条
  - 来源共 `5` 个：`录制内核`、`任务页`、`主窗口`、`任务存储`、`历史记录`
  - 级别统计包含 `信息` 与 `调试`
- 历史页联动已确认：
  - 已出现 `1` 条历史记录
  - 最新记录 `task-001`
  - 结果列 = `已停止`
  - 输出文件路径已展示到表格
- 本轮真实输出文件已落盘，且 `tasks.json`、`config.json`、`history.json` 均存在。

## 推断
- 从“新增任务 → 行内启动 → 运行中高亮 → 行内停止 → 历史落档 → 日志统计”的 GUI 主链路看，`BL-002` 的仓库内验收已经收口。
- 右键菜单的实际业务分发逻辑在当前验收中采用“状态快照 + 行内按钮实际执行”的方式旁证；对于仓库内功能边界，这已经足以证明菜单状态联动正确。

## 待确认
- 打包版 `dist/DouyinLiveRecorder Client/DouyinLiveRecorder Client.exe` 仍未做跨进程的真实 GUI 点按录制；当前仓库内只能确认它“可启动、可响应、会刷新 runtime_state”。
- 如果发布前必须保留“打包版 EXE 也要手点一轮”的门槛，这一步仍需要人工桌面操作或额外的跨进程 UI 自动化工具。

## 结论
- `BL-002` 已完成仓库内 GUI 收尾验收。
- 如无强制的“打包版 EXE 手点录制”发布门槛，后续优先级可以移回 `BL-004` NAS 宿主机实跑或客户端平台补证。
